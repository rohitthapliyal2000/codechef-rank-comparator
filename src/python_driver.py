import user_ranks as us

#user input
handle = input("Enter handle for user 1 : ")

dic1 = {}

us.details_for_one(handle, dic1)

handle = input("Enter handle for user 2 : ")

dic2 = {}

us.details_for_one(handle, dic2)
